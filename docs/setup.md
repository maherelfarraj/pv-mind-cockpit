# Setup

1. Enable pnpm through Corepack.
2. Install workspace dependencies with `pnpm install`.
3. Copy `.env.example` to `.env` and fill in project values.
4. Run `pnpm typecheck` and `pnpm build` to validate the scaffold.
