{
  "name": "@pv-mind/calculations",
  "version": "0.1.0",
  "description": "Core calculation functions for PV, BESS, yield, CAPEX, simulation, SCADA, and export locks",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": ["dist"],
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "typescript": "^5.4.0",
    "vitest": "^1.5.0"
  }
}
